{
  "name": "muu-platform",
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install && cd client && npm install && cd ../admin && npm install && cd ../service && npm install",
    "dev:client": "cd client && npm run dev",
    "dev:admin": "cd admin && npm run dev",
    "dev:service": "cd service && npm run start:dev",
    "build:client": "cd client && npm run build",
    "build:admin": "cd admin && npm run build",
    "build:service": "cd service && npm run build",
    "build:all": "npm run build:client && npm run build:admin && npm run build:service",
    "start:service": "cd service && node dist/src/main"
  }
}